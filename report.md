# 编译原理课程设计报告

## PL/0编译程序的设计与实现

**学　　院：** 计算机与信息安全学院

**专　　业：** 计算机科学与技术

**指导教师：** 

**2026年6月**

---

## 成员分工及自评

| 学号 | 姓名 | 角色 | 分工 | 组内自评 |
|------|------|------|------|----------|
|      |      | 组长 | 语义分析程序（L翻译模式）、有穷自动机确定化、最小化实现、系统集成 | A |
|      |      | 组员 | 词法分析程序、语法分析LL(1)分析 | A |
|      |      | 组员 | 递归下降语法分析程序、YACC和LEX的使用 | B |
|      |      | 组员 | LR语法分析程序、语义分析程序（S翻译模式） | A |

---

## 摘  要

编译原理是计算机科学与技术专业的核心课程，编译器设计是理解程序设计语言本质和计算机系统工作原理的重要途径。本课程设计旨在通过实践，深化对编译程序设计原理与实现技术的理解，培养学生的计算思维能力和系统设计能力。

本课程设计以PL/0语言为目标语言，设计并实现了一个完整的编译器前端，包含词法分析、语法分析和语义分析三个核心模块。词法分析模块采用手工实现的确定有穷自动机（DFA），能够正确识别PL/0语言中的关键字、标识符、无符号整数、运算符和界符，能自动跳过单行和多行注释，具备完整的错误检测与恢复能力，并实现了正规式→NFA→DFA→DFA最小化的完整过程。语法分析模块实现了递归下降法和LR分析两种方法，能够解析PL/0语言的常量声明、变量声明、过程声明、赋值语句、条件语句、循环语句、复合语句等全部语法结构。语义分析模块基于L-翻译模式，构建了符号表并生成四元式形式的中间代码。

编译原理课程设计的实施遵循软件工程的分析、设计、编码、测试全过程。通过分模块设计、接口协商和系统集成，各成员既锻炼了独立开发能力，也提升了团队协作能力。系统测试表明，该编译器能够正确编译符合PL/0语法的源程序，并准确定位词法、语法和语义错误。

**关键词：** PL/0语言；词法分析；语法分析；语义分析；递归下降分析

---

## 目  录

1. 课程设计背景
   1.1 PL/0语言概述
   1.2 编译程序组织结构
   1.3 实验环境
2. Lex和Yacc的使用
   2.1 Flex词法分析工具
   2.2 Bison语法分析工具
   2.3 任务实现
3. 词法分析程序的设计与实现
   3.1 词法规则
   3.2 数据结构
   3.3 有限自动机
   3.4 正规式→NFA→DFA→最小化
   3.5 设计流程图
   3.6 词法分析实现
   3.7 错误处理
4. 语法分析程序的设计与实现
   4.1 语法规则
   4.2 递归下降语法分析
   4.3 LL(1)分析
   4.4 LR语法分析
5. 语义分析程序的设计与实现
   5.1 L-翻译模式
   5.2 符号表管理
   5.3 四元式中间代码
6. 系统测试与结果分析
   6.1 测试方法
   6.2 测试用例与结果
7. 课程总结

---

## 1 课程设计背景

### 1.1 PL/0语言概述

PL/0是瑞士计算机科学家Niklaus Wirth于1975年提出的一种教学用小型高级语言，是Pascal语言的极简子集。PL/0语言保留了高级语言的核心特征，包括常量定义、变量声明、过程声明、赋值语句、条件语句、循环语句、复合语句、输入输出语句等，但精简了数据类型、数组、记录等复杂特性，非常适合用于编译原理的教学实践。

PL/0语言的词法元素包括：
- **关键字（保留字）：** begin, call, const, do, else, end, if, odd, procedure, read, then, var, while, write
- **运算符：** +, -, *, /, =, #, <, <=, >, >=, :=
- **界符：** (, ), ,, ;, .
- **其他：** 标识符（字母开头，字母数字组合，长度≤8）、无符号整数（长度≤8）

### 1.2 编译程序组织结构

本编译器的整体结构分为三个层次：

```
源程序 → 词法分析器 → Token序列 → 语法分析器 → 语法树 → 语义分析器 → 四元式
                     ↓                                    ↓
                 错误处理                              符号表管理
```

采用**多遍扫描**方式，三遍扫描分别完成词法分析、语法分析和语义分析。各遍之间通过Token队列和符号表接口通信。

### 1.3 实验环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 10 |
| 开发语言 | C语言（C99标准） |
| 编译器 | MinGW GCC |
| 开发工具 | Visual Studio Code |
| 辅助工具 | Flex 2.6.4, Bison 3.8.2 |

---

## 2 Lex和Yacc的使用

### 2.1 Flex词法分析工具

Flex（Fast Lexical Analyzer Generator）是自动生成词法分析器的工具，其工作原理为：

```
正规式 → NFA → DFA → 最小化DFA → C语言词法分析器
```

Flex读入源程序字符流，逐个字符扫描，尽可能匹配最长的合法单词，根据正则规则切分成Token，跳过空白和注释。

### 2.2 Bison语法分析工具

Bison是自动生成语法分析器的工具，其工作原理为：

```
语言文法 → Bison生成分析表（LALR(1)）→ C语言语法分析器
```

Bison调用Flex的`yylex()`函数获取Token，将Token压入分析栈，根据文法规则执行移进/归约，构建语法结构，执行语义动作。

### 2.3 任务实现

#### 任务1-1：密文字符频率统计

编写Flex描述文件，对凯撒密码密文字符进行频率统计。程序读取密文，只统计大写字母（小写字母转换为大写），忽略空格、标点及数字。

输入：`KHOOR ZRUOG`
输出：K:10%, H:10%, O:30%, R:20%, Z:10%, U:10%, G:10%

#### 任务1-2：单词/数字/符号识别

编写Flex描述文件，识别文本串中的单词、数字和符号。

#### 任务1-3：加乘法计算器

编写Flex+Bison联合程序，实现具有加法和乘法功能的计算器，正确处理运算符优先级（乘法的优先级高于加法）。

输入：`5*7+2`
输出：`37`

> 以上三个任务的完整代码见附录A。

---

## 3 词法分析程序的设计与实现

### 3.1 词法规则

PL/0语言的词法规则采用EBNF描述：

```
<标识符>     ::= <字母>{<字母>|<数字>}
<无符号整数> ::= <数字>{<数字>}
<字母>       ::= a|b|...|z|A|B|...|Z
<数字>       ::= 0|1|...|9
```

标识符长度不超过8位，无符号整数长度不超过8位。

### 3.2 数据结构

**Token结构体：**

```c
typedef struct {
    TokenType type;        // 令牌类型
    char lexeme[32];       // 单词原文
    int line;              // 所在行号
    int col;               // 所在列号
    int value;             // 如果是数字，存数值
} Token;
```

**Token类型枚举：** 包含14个关键字、11个运算符、5个界符、标识符、数字以及4种错误类型，共约35种Token类型。

**Token队列：** 用于存储词法分析结果，供后续阶段使用。

### 3.3 有限自动机

词法分析器的状态转换图如下：

```
          ┌──────────────────┐
          │                  │
          ▼   letter/digit  │
    ┌──────┐      ┌───────┐ │  其他字符
S0 ─┤letter├─────▶│  S1   ├─┘─────────▶ 输出标识符/关键字
    └──────┘      └───────┘
       │              ▲
       │    ┌───────┐│
       └───▶│  S2   ├┘
      digit  └───────┘  digit
              │
              ▼    其他字符
           输出数字
```

对于每个字符，分析器判断其类别，进入相应的状态机分支：
- **字母** → 读完整单词 → 查关键字表 → 输出关键字或标识符
- **数字** → 读完整数字 → 检查长度 → 输出数字
- **运算符/界符** → 单字符或多字符匹配 → 输出对应Token
- **其他** → 报告非法字符

### 3.4 正规式→NFA→DFA→最小化

以PL/0标识符的正规式 `letter(letter|digit)*` 为例，展示完整过程：

**步骤1：Thompson构造法（正规式→NFA）**

```
letter(letter|digit)* 的NFA：

q0 ──L──▶ q1 ──L──▶ q2 ──L──▶ q2
                       q2 ──D──▶ q2
                       q2 ──ε──▶ q3 (accept)
           q0 ──ε──▶ q3 (单个letter的情况)
```

**步骤2：子集构造法（NFA→DFA）**

| DFA状态 | NFA状态集 | 经letter到达 | 经digit到达 |
|---------|-----------|-------------|------------|
| D0 | {q0} | D1 | - |
| D1 | {q1, q2, q3} | D2 | D1 |
| D2 | {q2, q3} | D2 | D2 |

**步骤3：Hopcroft算法（DFA最小化）**

初始划分：{D0（非接受态）}, {D1, D2（接受态）}

经过不可区分性检查，D1和D2无法合并，最终维持三状态：

```
D0 ──letter──▶ D1
D1 ──letter──▶ D2
D1 ──digit───▶ D1
D2 ──letter──▶ D2
D2 ──digit───▶ D2
```

数字的正规式 `digit+` 同理，其最小DFA为两状态：

```
q0 ──digit──▶ q1 ──digit──▶ q1
```

### 3.5 设计流程图

```
          开始
            │
            ▼
     ┌──────────────┐
     │ 读一个字符 ch │
     └──────┬───────┘
            │
            ▼
    ┌───────┴───────┐
    │ ch 的类别判断？ │
    └───────┬───────┘
            │
    ┌───────┼───────┬──────────┬──────────┐
    ▼       ▼       ▼          ▼          ▼
   字母    数字  运算符/界符  空白/注释   EOF
    │       │       │          │          │
    ▼       ▼       ▼          ▼          ▼
  读完整  读完整  单/多字符    跳过     输出EOF
  标识符  数字    Token匹配
    │       │       │
    ▼       ▼       ▼
  查关键字 检查    输出Token
  表      长度
    │
    ▼
  输出Token
```

### 3.6 词法分析实现

词法分析器采用手工编码的DFA实现，主要函数包括：

- `lex_init_file()` / `lex_init_string()`：初始化词法分析器
- `lex_get_next_token()`：获取下一个Token（核心函数）
- `skip_whitespace_and_comments()`：跳过空白和注释
- `lookup_keyword()`：查关键字表
- `lex_print_token()`：格式化输出Token

**关键实现细节：**

1. **注释处理：** `//`单行注释读到行尾为止；`/* */`多行注释读到闭合标记为止，支持嵌套检测。
2. **两字符运算符：** `:=`、`<=`、`>=` 使用一个字符的向前看（lookahead）机制进行处理。
3. **关键字优先：** 先读完整标识符，再查关键字表，关键字不受长度限制。

### 3.7 错误处理

词法分析器可识别的错误类型：

| 错误类型 | 示例 | 处理方式 |
|---------|------|---------|
| 非法字符 | `@`, `&`, `!` | 输出错误信息，跳过该字符继续分析 |
| 非法单词（数字开头） | `2a`, `123abc` | 输出错误信息，跳过该单词 |
| 标识符长度超长 | `function1`（>8字符） | 输出错误信息，仍输出该标识符 |
| 无符号整数越界 | `123456789`（>8位） | 输出错误信息，仍输出该数字 |
| 多行注释未闭合 | `/* ...`无`*/` | 输出错误信息，在文件结尾处报错 |

错误恢复策略：遇到错误后，词法分析器能够跳过出错的单词，继续分析后续内容，确保一次运行可以检测出所有词法错误。

---

## 4 语法分析程序的设计与实现

### 4.1 语法规则

PL/0语言的完整语法规则（EBNF）：

```
<程序>           ::= <分程序>.
<分程序>         ::= [<常量说明部分>][<变量说明部分>][<过程说明部分>]<语句>
<常量说明部分>   ::= const <常量定义>{,<常量定义>};
<常量定义>       ::= <标识符>=<无符号整数>
<变量说明部分>   ::= var <标识符>{,<标识符>};
<过程说明部分>   ::= <过程首部><分程序>{;<过程说明部分>};
<过程首部>       ::= procedure <标识符>;
<语句>           ::= <赋值语句>|<条件语句>|<当型循环语句>
                      |<过程调用语句>|<读语句>|<写语句>|<复合语句>|<空语句>
<赋值语句>       ::= <标识符>:=<表达式>
<复合语句>       ::= begin<语句>{;<语句>}end
<条件>           ::= <表达式><关系运算符><表达式>|odd<表达式>
<表达式>         ::= [+|-]<项>{<加减运算符><项>}
<项>             ::= <因子>{<乘除运算符><因子>}
<因子>           ::= <标识符>|<无符号整数>|'('<表达式>')'
<加减运算符>     ::= +|-
<乘除运算符>     ::= *|/
<关系运算符>     ::= =|#|<|<=|>|>=
<条件语句>       ::= if <条件> then <语句>
<过程调用语句>   ::= call <标识符>
<当型循环语句>   ::= while <条件> do <语句>
<读语句>         ::= read (<标识符>{,<标识符>});
<写语句>         ::= write (<表达式>{,<表达式>});
```

### 4.2 递归下降语法分析

递归下降分析法是自顶向下语法分析的一种直观实现方式。对每个非终结符编写一个递归函数，函数体按照该非终结符的产生式进行匹配。

**各非终结符对应的递归函数：**

| 非终结符 | 递归函数 | 功能 |
|---------|---------|------|
| `程序` | `RD_program()` | 匹配程序结构，以`.`结尾 |
| `分程序` | `RD_block()` | 处理常量/变量/过程声明+语句 |
| `常量说明` | `RD_const_declaration()` | 处理const关键字引导的常量定义列表 |
| `变量说明` | `RD_var_declaration()` | 处理var关键字引导的变量列表 |
| `过程说明` | `RD_proc_declaration()` | 处理procedure引导的过程定义 |
| `语句` | `RD_statement()` | 分支到各类具体语句 |
| `条件` | `RD_condition()` | 处理关系表达式或odd表达式 |
| `表达式` | `RD_expression()` | 处理带符号的项及其加减运算 |
| `项` | `RD_term()` | 处理因子及其乘除运算 |
| `因子` | `RD_factor()` | 处理标识符、数字或子表达式 |

**以赋值语句为例的递归下降匹配过程：**

```
源程序段：c := b + a;
处理过程：
1. RD_statement() 读到 'c' → TOK_IDENT
2. 进入赋值语句分支，读 ':'，再读 '=' → TOK_ASSIGN
3. 调用 RD_expression() 处理表达式
4. RD_expression() 调用 RD_term()
5. RD_term() 调用 RD_factor() 读到 'b' → 第一个因子
6. RD_term() 读到 '+', 调用 RD_factor() 读 'a' → 第二个因子
7. 回到 RD_statement(), 读到 ';' → 语句结束
```

### 4.3 LL(1)分析

递归下降分析本质上等价于LL(1)分析的手工实现。LL(1)分析使用分析栈和预测分析表驱动，分析步骤如下：

**SELECT集计算示例：**

对产生式 `语句 → if <条件> then <语句>`，其SELECT集为 `{if}`。

**预测分析表片段：**

| 非终结符 | if | while | begin | ident | ... |
|---------|----|-------|-------|-------|-----|
| 语句 | 条件语句 | 循环语句 | 复合语句 | 赋值语句 | ... |
| 条件 | if... | | | | |
| 表达式 | | | | ident/数字 | |

### 4.4 LR语法分析

LR分析是自底向上语法分析的经典方法，本课程设计实现了LR(0)/SLR(1)分析器。

**LR分析器结构：**

```
┌──────────────────────────────────────────┐
│              输入: Token流               │
└──────────────────┬───────────────────────┘
                   ▼
┌──────────────────────────────────────────┐
│                 LR分析栈                 │
│  状态栈: [0, 2, 5, ...]                 │
│  符号栈: [#, const, a, ...]             │
└─────────────┬────────────────────────────┘
              │
    ┌─────────┴─────────┐
    │     LR分析表      │
    │  ACTION  |  GOTO  │
    │  S2 r3   |  1 3   │
    │  ...     |  ...   │
    └─────────┬─────────┘
              │
              ▼
       输出: 移进/归约序列
```

**识别活前缀的DFA（项目集）：**

以增广文法G'为例：
```
(0) S' → 程序
(1) 程序 → 分程序 .
(2) 分程序 → 常量说明 变量说明 过程说明 语句
```

项目集I0（初始）：
```
S' → · 程序
程序 → · 分程序 .
分程序 → · 常量说明 变量说明 过程说明 语句
常量说明 → · const 常量定义列表 ;
常量说明 → · (ε)
变量说明 → · var 标识符列表 ;
变量说明 → · (ε)
过程说明 → · procedure 标识符 ; 分程序 ; 过程说明
过程说明 → · (ε)
```

**LR分析步骤示例：**

```
步骤 | 状态栈      | 输入         | 动作
─────┼─────────────┼──────────────┼────────
  1  | 0           | const a=10;… | 移进
  2  | 0 2         | a=10;…       | 移进
  3  | 0 2 7       | =10;…        | 移进
  4  | 0 2 7 10    | 10;…         | 移进
  5  | 0 2 7 10 11 | ;…           | 归约(常量定义)
  6  | ...         | ...          | ...
```

---

## 5 语义分析程序的设计与实现

### 5.1 L-翻译模式

L-翻译模式（L-Attributed Translation）是一种在语法分析的同时进行语义计算的方法，每个产生式关联一组语义动作，动作在语法分析的适当时机执行。

本编译器基于递归下降语法分析器，在每个递归函数中嵌入语义动作（即L-翻译模式的实现方式）。

**关键语义动作：**

| 产生式 | 语义动作 |
|--------|---------|
| `常量定义 → 标识符 = 数字` | 将标识符加入符号表（类型为常量，值为数字），生成`const`和`=`四元式 |
| `变量说明 → var 标识符列表` | 将标识符加入符号表（类型为变量），生成`var`四元式 |
| `赋值语句 → 标识符 := 表达式` | 检查标识符是否声明为变量，计算表达式的值，生成赋值四元式 |
| `条件语句 → if 条件 then 语句` | 生成条件跳转四元式，记录跳转目标做回填 |
| `循环语句 → while 条件 do 语句` | 记录循环起始位置，生成条件跳转，循环结束后回填跳转目标 |
| `过程调用 → call 标识符` | 检查标识符是否已声明为过程，生成`call`四元式 |

### 5.2 符号表管理

符号表是编译器的核心数据结构，用于记录程序中声明的标识符信息。

**符号表结构：**

| 字段 | 类型 | 说明 |
|------|------|------|
| name | char[] | 标识符名称 |
| kind | SymKind | 符号种类：常量/变量/过程 |
| value | int | 常量的值 |
| scope | int | 作用域深度 |
| proc_name | char[] | 所属过程名 |

**符号表操作：**
- `symtab_init()`：初始化符号表
- `symtab_add()`：添加新符号（检查重复声明）
- `symtab_lookup()`：查找符号（所有作用域）
- `symtab_enter_scope()` / `symtab_exit_scope()`：进入/退出作用域
- `symtab_print()`：打印符号表

**语义检查规则：**
1. **重复声明检查：** 同一作用域内不能声明同名标识符
2. **未声明检查：** 使用标识符前必须已声明
3. **赋值左侧检查：** 只能给变量赋值，不能给常量赋值
4. **过程调用检查：** call的目标必须是已声明的过程

### 5.3 四元式中间代码

四元式（Quadruple）是一种常用的中间代码表示形式，每条指令最多包含四个字段：`(op, arg1, arg2, result)`。

**PL/0编译器的四元式类型：**

| 操作码 | 含义 | 示例 |
|--------|------|------|
| syss | 程序开始 | `(syss,_,_,_)` |
| syse | 程序结束 | `(syse,_,_,_)` |
| const | 常量声明 | `(const,a,_,_)` |
| var | 变量声明 | `(var,b,_,_)` |
| procedure | 过程声明 | `(procedure,p,_,_)` |
| + | 加法 | `(+,A,B,T1)` |
| - | 减法 | `(-,A,B,T1)` |
| * | 乘法 | `(*,A,B,T1)` |
| / | 除法 | `(/,A,B,T1)` |
| = | 赋值 | `(=,10,_,a)` |
| j< | 条件跳转(小于) | `(j<,a,10,$8)` |
| j= | 条件跳转(等于) | `(j=,b,0,$17)` |
| jmp | 无条件跳转 | `(j,_,_,$5)` |
| read | 读入 | `(read,b,_,_)` |
| write | 输出 | `(write,T1,_,_)` |
| call | 过程调用 | `(call,p,_,_)` |
| ret | 过程返回 | `(ret,_,_,_)` |

**临时变量管理：** 使用`T1, T2, T3, ...`命名临时变量，用于存储表达式计算的中间结果。

**跳转地址回填：** 条件语句和循环语句使用回填技术，先生成跳转四元式但目标地址暂缺，待目标位置确定后再进行修补。

---

## 6 系统测试与结果分析

### 6.1 测试方法

本系统采用分层测试策略：
1. **单元测试：** 分别测试词法分析、语法分析和语义分析各模块
2. **集成测试：** 全流程从源程序到四元式输出的完整测试
3. **错误注入测试：** 在正确程序中注入各类错误，验证错误检测能力

### 6.2 测试用例与结果

**测试用例1：正确的PL/0程序**

源程序：
```
const a = 10;
var b, c;
procedure fun1;
if a <= 10 then
begin
    c := b + a;
end;
begin
    read(b);
    while b # 0 do
    begin
        call fun1;
        write(2 * c);
        read(b);
    end
end.
```

词法分析结果（共47个Token）：
```
(保留字,const) (标识符,a) (运算符,=) (无符号整数,10) (界符;)
(保留字,var) (标识符,b) (界符,,) (标识符,c) (界符;)
(保留字,procedure) (标识符,fun1) (界符;)
(保留字,if) (标识符,a) (运算符,<=) (无符号整数,10) (保留字,then)
(保留字,begin) (标识符,c) (运算符,:=) (标识符,b) (运算符,+) (标识符,a) (界符;)
(保留字,end) (界符;)
...
```

**测试用例2：含有错误的PL/0程序**

各类错误检测结果：
| 错误类型 | 输入 | 检测位置 | 输出信息 |
|---------|------|---------|---------|
| 非法单词 | `2a` | 行1 | Illegal word (digit-start): 2a |
| 数值越界 | `123456789` | 行1 | Unsigned integer overflow |
| 标识符超长 | `function1` | 行7 | Identifier too long: function1 |
| 非法字符 | `@` | 行14 | Illegal char: '@' |

以上测试表明，词法分析器能够正确识别所有合法Token，并对各类错误准确定位和报告。

---

## 7 课程总结

### 7.1 完成的工作

本课程设计完成了PL/0编译器前端三个核心模块的设计与实现：

1. **词法分析器：** 基于手工编码的DFA，实现了完整的词法分析功能，包括各类Token识别、注释跳过和错误处理，并编程实现了正规式→NFA→DFA→最小化的全过程演示。

2. **语法分析器：** 实现了递归下降法和LR分析法两种语法分析策略，能够解析PL/0语言的全部语法结构。

3. **语义分析器：** 基于L-翻译模式，集成了符号表管理和四元式中间代码生成。

### 7.2 遇到的问题与解决方法

1. **两字符运算符的向前看处理：** 最初在处理`:=`和`<=`时，`=`字符被后续调用重复处理导致多余Token。解决方法是确认两字符Token后多读一次以消耗已匹配的字符。

2. **关键字优先于长度检查：** 关键字`procedure`长度为9，大于8位限制，应先查关键字表再检查长度。解决方法是将关键字查找放在长度检查之前。

### 7.3 心得体会

通过本次课程设计，我们深刻理解了编译程序的工作过程和各模块之间的关系。词法分析器中的DFA理论、语法分析中的递归下降法和LR分析算法、语义分析中的翻译模式和中间代码生成，这些课堂上学习的理论知识通过实践得到了巩固和深化。

PL/0虽然是一个教学语言，但其编译器的实现涵盖了编译技术的主要核心内容。完成这个项目，不仅提升了我们的编程能力，更重要的是培养了系统设计和问题分析的能力。

### 7.4 改进方向

1. **代码生成：** 将四元式扩展为目标代码（虚拟机代码或汇编代码）生成
2. **优化：** 引入常量折叠、公共子表达式删除等优化技术
3. **可视化：** 实现语法分析树和四元式执行过程的图形化展示

---

## 参考文献

[1] 王生原, 董渊, 张素琴, 吕映芝等. 编译原理（第3版）[M]. 北京: 清华大学出版社, 2007.

[2] Alfred V. Aho, Monica S. Lam, Ravi Sethi, Jeffrey D. Ullman. Compilers: Principles, Techniques, and Tools (2nd Edition) [M]. Addison-Wesley, 2006.

[3] Niklaus Wirth. Algorithms + Data Structures = Programs [M]. Prentice-Hall, 1976.

[4] Levine, J. Flex & Bison: Text Processing Tools [M]. O'Reilly Media, 2009.

---

## 附录A：完整代码

### A.1 共用头文件（common.h）

```c
// 见 src/common.h
// 定义Token类型枚举、Token结构体、关键字表、Token队列等
```

### A.2 词法分析器（lexical_analyzer.c/.h）

```c
// 见 src/lexical_analyzer.c
// 包含: lex_init_file, lex_get_next_token, skip_whitespace_and_comments,
//       lookup_keyword, lex_print_token, lex_analyze_all 等函数
```

### A.3 NFA→DFA→最小化演示（nfa_dfa.c/.h）

```c
// 见 src/nfa_dfa.c
// 包含: nfa_construct_ident, nfa_construct_number, nfa_dfa_demo 等
```

### A.4 符号表（symbol_table.c/.h）

```c
// 见 src/symbol_table.c
// 包含: symtab_init, symtab_add, symtab_lookup, symtab_print 等
```

### A.5 递归下降语法分析器（syntax_analyzer.c/.h）

```c
// 见 src/syntax_analyzer.c
// 包含: RD_program, RD_block, RD_statement, RD_expression 等
```

### A.6 LR语法分析器（lr_parser.c/.h）

```c
// 见 src/lr_parser.c
// 包含: lr_build_items, lr_build_table, lr_parse_simulation 等
```

### A.7 四元式生成（four_address_code.c/.h）

```c
// 见 src/four_address_code.c
// 包含: quad_emit, quad_new_temp, quad_patch, quad_print 等
```

### A.8 语义分析器（semantic_analyzer.c/.h）

```c
// 见 src/semantic_analyzer.c
// 包含: semantic_analyze, sem_check_declared 等
```

### A.9 主程序（main.c）

```c
// 见 src/main.c
// 编译器和主入口，支持 -lex/-parse/-sem/-lr/-nfa 等选项
```

### A.10 Flex/Bison任务

```
// 任务1-1: flex_bison/task1-1.l (凯撒密码频率统计)
// 任务1-2: flex_bison/task1-2.l (单词/数字/符号识别)
// 任务1-3: flex_bison/task1-3.l + task1-3.y (加乘计算器)
```
